# Gitlab CI 
# Build and Test pipeline for Python

image: "python:3.7"

before_script:
  - python --version
  - pip install pylint
  - pip install -r requirements.txt

stages:
  - Test

pylint:
  stage: Test
  script:
    - pylint -d C0301 src/*.py
  allow_failure: true