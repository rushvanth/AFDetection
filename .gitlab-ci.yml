# Gitlab CI 
# Build and Test pipeline for Python

image: "rushvanth/ds-python"

before_script:
  - python --version
  - pip install -r requirements.txt

stages:
  - Test

pylint:
  stage: Test
  script:
    - pylint src/*.py
  allow_failure: true